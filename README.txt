git pull                         // Pull other changes that is not on your branch
git status                       // Check the your files with changes
git add
   git add -A                    // -A means to add all files with changes
   git add file_path             // Add a specific file only "git add landing-page-1/index.html"
git commit -m "commit message"   //Please write a descriptive commit message
git push                         // Push the changes live to the Master branch
